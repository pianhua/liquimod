# LiquiMod UI 设计与交互规范

> 当前基线：v0.5.1 · Svelte 5 + Tailwind CSS v4 + Tauri 2 + WebView2
>
> 目标：在亮色、暗色、角色立绘半透明和高亮背景同时存在时，每一个可操作元素都清楚、稳定、可预测。

本文档约束 LiquiMod 的视觉、交互和 UI 验收。新增功能、修复问题和重构组件都必须遵守；确需偏离时，必须在任务或 PR 中说明原因、风险和替代方案。

## 1. 设计原则

1. 可读性优先于玻璃效果。毛玻璃、立绘和环境光不能牺牲文字、按钮边界、开关状态或警告信息。
2. 同层同规格。同一工具栏、列表或弹窗中的控件使用同一高度、圆角、内边距和字级。
3. 状态必须可见。启用、禁用、悬停、按下、聚焦、离线、游戏运行中和危险状态不能只依靠颜色区分。
4. 交互必须可恢复。弹窗、菜单、拖拽和异步操作都要有明确的取消、关闭、失败反馈和最终状态。
5. 亮暗主题同等重要。所有 UI 改动至少检查 Light、Dark；涉及立绘时还要检查亮、暗、透明图片。

## 2. 颜色与主题 Token

所有颜色优先使用 app/src/app.css 中的 CSS 变量。组件中禁止新增硬编码 #hex、rgb() 或固定白黑色作为主要 UI 颜色。

| Token | 语义 |
| --- | --- |
| --surface | 页面基础背景 |
| --text | 主标题、正文和主要按钮文字 |
| --text-secondary | 元数据、辅助说明和次级文字 |
| --accent / --accent-fill | 主操作、选中、聚焦和链接 |
| --danger | 删除、卸载、不可恢复操作和错误 |
| --warning / --warning-fill / --warning-stroke | 多 Mod 风险、离线源和需要注意的状态 |
| --glass-bg / --glass-stroke | 普通玻璃容器 |
| --glass-floating-bg / --glass-floating-stroke | 菜单、弹窗和需要隔离背景的浮层 |
| --panel-bg / --card-bg | 高可读性面板和卡片底色 |
| --item-hover / --item-active | 列表项悬停和按下反馈 |
| --input-bg | 输入框、代码和日志区域底色 |

新增 Token 前先确认现有变量不能表达该语义。主题覆盖必须同时更新默认、系统暗色和显式 data-theme="dark" 分支。

## 3. 几何与尺寸

### 3.1 圆角

| 类名 | 规格 | 用途 |
| --- | ---: | --- |
| radius-window | 26px | 应用主窗口外轮廓 |
| radius-panel | 20px | 面板、弹窗、浮层和安装进度面板 |
| radius-card | 18px | 角色卡、Mod 行、详情卡和统计卡 |
| rounded-lg | 8px | 下拉菜单项和列表内矩形项目 |
| radius-pill | 9999px | 按钮、筛选器、搜索框、状态胶囊和 Toggle |

除菜单内部条目等明确矩形语义外，不要随意引入新的圆角等级。

### 3.2 控件高度

| 档位 | 高度 | 用途 |
| --- | --- | --- |
| 主控 | h-9 | 顶部主搜索和页面级主动作 |
| 标准 | h-8 | 工具栏、保存、导入、全开/全关和普通图标按钮 |
| 微控 | h-7 | 筛选器、紧凑标签和菜单内辅助动作 |

默认图标操作按钮使用 w-8 h-8 glass radius-pill，并提供有意义的 aria-label。危险图标按钮需要危险颜色、确认动作或明确撤销路径。

## 4. 组件与交互

### 4.1 Toggle 与按钮

- 所有开关使用 $lib/components/Toggle.svelte，不得自制 checkbox。
- 按下使用全局 active 缩放反馈；禁用时保持布局，不出现跳动、文字消失或仍可点击。
- 异步操作期间按钮必须禁用或显示进行中状态，避免重复提交。
- 纯图标按钮必须有 aria-label；复杂操作可使用全局 Tooltip。
- 表单控件使用 focus-visible 光晕，不能移除键盘焦点可见性。

### 4.2 菜单、弹窗和浮层

- 菜单、右键菜单和弹窗使用 glass-floating 或 glass-menu，不能以透明背景直接叠在立绘上。
- 浮层祖先需要明确定位和层级，例如 relative z-30；固定浮层不能被带 transform 的卡片遮挡。
- 菜单至少支持点击外部、Escape 或执行动作中的一种关闭方式；重要弹窗应支持三种。
- 指针离开后必须清除悬停预选效果，不能残留阴影或手动高亮 class。
- 弹窗确认和取消都必须可操作，破坏性操作必须明确告知影响范围。

### 4.3 Toast、通知和异步反馈

- 成功、警告、错误和进行中状态使用统一 Toast/Overlay，不在页面内私造通知样式。
- 文案要说明做了什么和下一步怎么办；外部 Mod 断开必须明确说明源文件未删除。
- 错误不能只显示颜色或图标，必要时带路径、操作名和恢复建议。
- 安装、迁移、扫描和清理等长任务显示阶段，失败后保留可读错误。

## 5. 立绘、玻璃与可读性

这是 LiquiMod 的重点验收项。

1. 标题、按钮、菜单文字和警告信息不能直接压在不可控立绘上，应放在 glass、glass-floating、panel-bg 或带遮罩的区域内。
2. 下拉菜单、弹窗、右键菜单和 Tooltip 使用高不透明度浮层背景，不能让亮色立绘穿透文字。
3. 黄色信号灯、离线徽章和冲突提示必须同时有图标、边框或文案，不能只显示一小块颜色。
4. 图片缩放、裁切和透明图层不得覆盖关闭按钮、分页按钮、标题或关键说明。
5. 至少验证白色立绘、黑色立绘、透明 PNG、强高光图片和无预览图。
6. Light 下不能出现浅灰字或白边融入白背景；Dark 下不能出现边缘发灰、描边过亮或警告颜色失真。

## 6. 页面验收范围

每页逐项检查：

- 标题、返回、搜索、筛选和排序；
- 每个卡片、列表行、Toggle、图标按钮和右键菜单项；
- 弹窗确认、取消、关闭、外部点击和 Escape；
- 成功、失败、离线、禁用、加载中和空状态；
- Light/Dark 文字、边框、背景和立绘叠加；
- 滚动、浮层层级、窗口缩放和键盘焦点；
- 游戏运行时允许/禁止操作是否与后端一致。

重点页面：角色列表、角色详情、Mod 详情、设置、分类菜单、排序菜单、预设菜单、右键菜单、安装进度 Overlay 和 Toast。

## 7. 自定义排序拖拽

自定义排序使用 Pointer Events，不使用 HTML5 原生拖拽。

- pointerdown 记录行矩形、指针相对偏移和当前顺序。
- pointermove 只更新浮层和插槽顺序，不让真实列表二次位移。
- 预览在独立 overlay host 中定位，使用相对 host 坐标。
- pointerup、pointercancel 和失焦都清理监听器、预览和拖动状态。
- 松手后真实列表只提交一次排序，不能上弹、抖动、错位或短暂回到旧位置。
- 拖动禁用文本选择，但不能影响 Toggle、菜单和其他子控件点击。

修复拖拽时检查顶部、中部、底部、滚动后列表、首末项互换和亮暗主题。

## 8. 视图状态与导航

- viewSearchMemory 按视图 key 保存搜索词；列表进入详情再返回必须恢复搜索和过滤结果。
- 切换视图或打开全屏设置前保存滚动位置，渲染完成后恢复。
- 异步刷新使用单调序号、取消机制或等价保护，旧请求不得覆盖新状态。
- 浮层层叠、滚动和网格布局必须在 WebView2 下验证，不能只凭桌面 Chromium 表现。

## 9. UI PR 审查清单

- [ ] 使用现有 Token、glass/glass-floating、圆角和高度体系。
- [ ] 图标按钮有 aria-label，异步按钮有禁用/进行中反馈。
- [ ] Toggle 使用统一组件。
- [ ] Light、Dark、亮立绘、暗立绘和透明立绘均检查。
- [ ] 菜单离开后没有残留 hover/预选阴影。
- [ ] 拖拽没有上弹、抖动或坐标错位。
- [ ] 弹窗、Toast、空状态、错误状态和离线状态可读。
- [ ] 相关测试已更新，并通过 npm run check 与 npm test。
