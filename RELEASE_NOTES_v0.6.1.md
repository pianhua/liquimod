# LiquiMod v0.6.1

## 修复

- 修复顶部“热重载”发送 F10 后游戏不响应、必须手动按物理键盘 F10 的问题。
- F10 刷新助手改用 F10 硬件扫描码 `0x44`，分离发送按下与抬起事件，并增加窗口聚焦等待。
- 增加 F10 helper 的管道、窗口、进程、`SendInput` 和确认结果日志，便于后续排查权限或游戏兼容问题。

## 当前产品决策

- 设置页“外部 Mod 源目录”批量扫描暂缓；角色详情页“连接外部”作为当前主入口。
- Hash/变量冲突检测接口保留，但当前不自动扫描、不阻止、不互斥、不停用 Mod。
- 详细决策与重新启动条件见 [DECISIONS.md](DECISIONS.md)。

## 验证

- F10 修复已完成用户实机验收：热重载正常生效。
- Rust workspace 测试、Clippy、前端检查、前端测试和 Windows release 打包在发布前执行。
