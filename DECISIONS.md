# LiquiMod 产品决策记录

> 本文件记录会影响产品边界、数据行为或后续开发方向的已确认决策。
> 临时讨论不能替代本文件；重新启动已暂缓功能前，必须先更新这里并完成需求对齐。

## D-001：暂缓设置页外部 Mod 源目录批量扫描

- **状态**：Deferred（暂缓）
- **确认日期**：2026-08-22
- **追踪 Issue**：[pianhua/liquimod#13](https://github.com/pianhua/liquimod/issues/13)
- **当前主入口**：角色 Mod 详情页的“连接外部”

### 决策

设置页的“外部 Mod 源目录”批量扫描暂不作为当前主要使用流程。角色详情页直接选择一个完整 Mod 文件夹并连接，作为普通用户的外部 Mod 入口。

### 原因

现有设置页扫描只覆盖有限目录层级，缺少递归范围、扫描结果预览、失败原因、逐项确认和批量启用反馈。继续扩展前，无法保证“添加任意外部目录即可可靠发现 Mod”的产品承诺。

### 保留与边界

- 保留外部 Mod 的索引、规范化源路径、数据库字段和后端 API。
- 外部源文件仍不得被复制、移动、接管或删除。
- 角色详情页直连不因本决策停用。
- 暂不删除设置页后端能力，避免破坏已有配置和未来迁移路径。

### 重新启动条件

重新开发时，应先对齐并实现：可配置递归深度和忽略规则、扫描结果预览、失败原因、逐项确认、批量启用，以及源目录与具体 Mod 的清晰关系。

## D-002：暂缓 Hash/变量冲突诊断与自动策略

- **状态**：Deferred（暂缓）
- **确认日期**：2026-08-22
- **追踪 Issue**：[pianhua/liquimod#10](https://github.com/pianhua/liquimod/issues/10)

### 决策

当前不自动执行 Hash 冲突扫描，不根据 Hash 或变量冲突阻止、互斥或停用 Mod。核心层已有的解析、检测和变量隔离接口继续保留，但不作为当前启停流程的强制门禁。

### 原因

真实 3Dmigoto Mod 的 `d3dx.ini`、Hash、变量和变体结构差异较大。没有足够真实样本和明确的非侵入式诊断 UI 前，自动拦截容易产生误报并改变用户现有 Mod 行为。

### 重新启动条件

重新开发时，应先收集真实 Mod 样本，明确 Hash/变量冲突的展示范围、误报处理、用户确认方式和是否允许仅提示而不拦截；任何自动停用或互斥策略都需要单独确认。

## 维护规则

- 新的产品取舍先写入本文件，再进入实现或发布说明。
- 已暂缓事项不能只保留在聊天记录中；必须同时有交接记录和可追踪 Issue。
- 重新启动暂缓事项时，先更新状态、目标和验收条件，再开独立开发分支。
