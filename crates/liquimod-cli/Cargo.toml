[package]
name = "liquimod-cli"
version = "0.1.0-beta.3"
edition = "2021"

[dependencies]
liquimod-core = { path = "../liquimod-core", version = "0.1.0-beta.3" }
clap = { version = "4", features = ["derive"] }
rpassword = "7.5.4"
